object q1andq2 extends App{
    var k , i, j: Int = 2;
    val m, n:Int = 5;
    var f = 12.0f;
    var g = 4.0f;
    var c = 'X';

    println(k + 12 * m)
    println(m / j)
    println(n % j)
    println(m / j * j)
    println(f + 10*5 +g)
    //println(++i * n)   //incremental operators are not supported by scala  

    i= i + 1;
    println(i*n);
}